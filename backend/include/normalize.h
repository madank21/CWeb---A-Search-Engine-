#ifndef CWEB_NORMALIZE_H
#define CWEB_NORMALIZE_H

/* Applies ASCII light suffix-stripping normalization in-place on a token string. */
void normalize_token(char *token);

#endif /* CWEB_NORMALIZE_H */
